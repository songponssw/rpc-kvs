apiVersion: apps/v1
kind: Deployment
metadata:
  name: tracing-server
spec:
  selector:
    matchLabels:
      app: trc
  template:
    metadata:
      labels:
        app: trc
    spec:
      containers:
        - name: tracing-server
          image: docker.io/kofeebrian/kvs-tracing-server:k8s
          resources:
            limits:
              memory: "128Mi"
              cpu: "500m"
          ports:
            - containerPort: 6666
          volumeMounts:
            - mountPath: /
              name: tracer-log
      volumes:
        - name: tracer-log
          emptyDir: {}
